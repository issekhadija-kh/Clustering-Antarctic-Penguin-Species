# Clustering-Antarctic-Penguin-Species

This notebook applies unsupervised machine learning to identify penguin species groupings from physical measurements collected at Palmer Station, Antarctica. Since species labels are unavailable in the dataset, K-Means clustering is used to discover natural groups corresponding to the three known native species: Adelie, Chinstrap, and Gentoo.
