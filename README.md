# Clustering-Antarctic-Penguin-Species

This project applies unsupervised learning to the classic Palmer Penguins dataset. The goal is to group penguins into clusters based on their physical measurements, without using the species labels. After clustering, we compute the average values of the original numeric features for each cluster.
